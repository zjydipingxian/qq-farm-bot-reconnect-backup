# 萌宠成长日记适配与本地验证（2026-09-10）

接入活动目录、比熊之家、爪印手记、拾物小铺、节令小礼、宝藏护送和游戏商城的活动商品筛选。本文记录上游活动分支的实现、协议来源与本地验证范围。

## 协议与资源来源

- 协议依据活动真实抓包及官方小程序 encoder / decoder；用于回归的消息体与预期字段保存在 `core/tests/fixtures/pet-*.json`。
- 当前 QQ 小程序 `1.14.0.3`，实际登录版本 `1.14.0.3_20260909`。
- 活动分组 `2026090100`，主活动 `2026090101`，种子赠礼 `2026090102`，拾物小铺 `2026090103`。
- `core/src/proto/pet-diary.proto` 按官方 encoder / decoder 的字段号、类型及嵌套消息实现。真实请求与编码结果逐字节一致，真实响应的全部已定义字段与官方 decoder 逐项比较；未使用响应字节数推断含义。
- 活动业务图像位于 `web/public/activity-assets/pet-diary/`。`core/src/activity-data/pet-diary-assets.json` 记录 27 项资源的原始路径、官方 CDN URL、原始 PNG 的 SHA-256、本地 WebP 的 SHA-256 与编码参数；所有 `sha256` 与本地文件逐一匹配。
- 手记页改成自绘纸感卡片墙：未解锁用虚线占位加爪印，解锁后显示服务端下发的手记照片（`img_s3PhotoWall_photo*`）。页内原先自拟的装饰文案已移除。
- 照片相框按原图比例 `aspect-ratio: 539 / 774` 固定，图片用 `object-fit: contain` 完整显示，不再裁切；相框高度随比例确定，修复图片被网格固有高度撑大后裁掉下半部分的问题。

## 界面设计（2026-09-11 自绘改版）

- 页面改为一套自绘的奶油纸感界面：`#fffdf8` 纸底、蜂蜜棕强调色（`#d59a4f` / `#a9762c`），颜色全部取自 `web/src/style.css` 的 `--ui-*` 变量，不再平铺官方背景图。
- 比熊、爪印、节令插画和护送场景都改成内联 SVG 组件（`PetMascot.vue`、`PetPaw.vue`、`PetSolarScene.vue`、`PetEscortLandscape.vue`）；活动列表里萌宠日记的入口图标同样改用 `PetMascot`，不再引用 `S3Open_dog_1.png`。
- 这一版删除了此前由 Spine 导出并在页面里平铺的官方素材：五张 `scene-*` 静态图与三张 WebP 动图、`img_s3Treasure_*` 护送 UI、`img_s3BattlePass_*`、`img_s3main_*`、`img_S3Shop_*`、`img_S3Jieqi_*`、`img_pet_rarity*` 以及 `escort-dog-*` / `escort-wheel` 帧。生产依赖不再需要 Pixi 和 Spine 运行时。
- 只保留服务端会下发的业务图：手记照片 `img_s3PhotoWall_photo0-14`、锦囊图标 `img_s3_skillType1-5`、小铺商品图 `img_exchange_item1-7`，共 27 项。它们在本地改用 WebP 保存：照片有损 `quality=82, method=6`（与原 PNG 的 PSNR 为 35.7–39.3 dB），图标与小铺商品图无损 `method=6`。资源目录从 132 项、40.29 MB 降到 27 项、1.15 MB。
- `core/src/activity-data/pet-diary-assets.json` 同步收敛到 27 项（手记文字图 `img_s3PhotoWall_say*` 随卡片改版移除，其原始 CDN 地址与哈希仍可从 git 历史中的旧清单取回）：`file` 指向本地 WebP，新增 `sourceFile` / `sourceSha256` 记录原始 PNG 名称与哈希，`sha256` 改为本地文件哈希，`encoding` 记录编码参数。`core/src/services/activity-center/pet-diary.ts` 里 `assetPaths` 的映射逻辑未改。
- 布局约束：`.pet-diary` 用 flex 纵向排布，正文在 `.pet-body` 内独立滚动，底部栏目 `.pet-nav` 在滚动区之外并紧贴容器底边（390px / 320px 实测栏目底边与页面底边间距为 0，无留白）。固定尺寸元素一律给死 `height` / `min-height`（底部按钮 52px、照片框 `aspect-ratio: 539 / 774` + `object-fit: contain`），窄屏改用单列网格而不是压缩元素高度。
- 响应式断点：≤1023px 比熊之家与节令卡片转单列，≤720px 手记两列、小铺两列、主按钮撑满整行，≤420px 页头收起“活动列表”文字和剩余时间前缀。
- 小铺点击商品先打开兑换弹窗，展示数量、总成本、余额和剩余限购，只有点击明确的“兑换”按钮才提交；手记继续使用独立领奖按钮。
- **不上传合成输入和工具**：Spine 原始 `.skel`、骨骼 JSON、`.atlas`、整张图集和导出依赖均放在 `output/`；该目录及 `.playwright-cli/` 已加入 `.gitignore`。旧的 14 张手工拼层切片也移回本地目录。生产依赖未新增 Pixi、Spine 或 GIF 编码器，正式资源目录没有骨骼和图集配置文件。
- 2026-09-11 布局与入口微调：`ActivityShell` 在桌面端改为 `height: 100%` + `min-height: 0` 跟随父容器。原先的 `calc(100dvh - 48px)` 与 `min-height: 620px` 比页面实际可用高度多出约 68px，会把 `.pet-nav` 挤出可视区；实测 1024–1920 宽度下 `.page-scroll` 不再溢出，栏目完整可见。≤1023px 的高度公式不变。
- 好友夺宝卡片默认展开（`<details open>`），活动说明仍默认收起。
- 爪印手记卡片简化为“照片 + 第几则 + 领取按钮”：删掉领取按钮上方的文字图 `img_s3PhotoWall_say*`，15 张 WebP 与接口字段 `captionImage` / `caption` 一并移除（上游 `selected_desc.say` 只下发图片路径，从未下发文字，该字段无可显示内容）；照片按原图比例完整展示。
- 移除两处纯跳转入口：拾物小铺的“前往游戏商城”、比熊之家萌宠元气糕卡片的“前往农场种植”；页面内随之不再使用 `useRouter`。萌宠元气糕卡片删掉入口后只剩说明文字，内容与服务端下发的活动说明重复，整卡一并移除。

改版后正式资源清单只描述保留的 42 项业务图，历史 Spine 成品的复现参数不再随应用发布。

## 活动交互

- 活动名称统一为“萌宠成长日记”，仅“比熊之家”显示活动说明。幼年和成年主按钮均为“投喂元气糕”；幼年发送官方 FEED（29），成年发送 DRAW（30），获得宝藏后由服务端自动开启护送。
- 互动记录通过可滚动弹窗查看；护送弹窗的关闭、返回图标垂直居中。操作成功提示 4 秒后消失，切换账号、栏目和重新打开弹窗时清理旧提示，错误提示保留以便处理。
- 比熊、乐园变异的名称、图标以及泡泡棉花糖、比熊棉花糖和黄金组合按官方配置核对，并加入回归测试。

## 真实操作证据

| 抓包 | 操作 | 已验证结果 |
| --- | --- | --- |
| `000156/000157` | 领养初始化 | 返回幼年比熊状态 |
| `000275/000276` | 种子赠礼 | 泡泡棉花糖种子 `29004 ×1`、狗尾草种子 `20516 ×32` |
| `000309/000310` | 元气糕不足时投喂 | 错误 `1000019`，服务端提示“萌宠元气糕不足” |
| `000317/000318` | 拾物小铺 | 13 件商品及真实价格、限购总数、已购次数 |
| `000369` | 白露小礼 | 芦苇种子 `25995 ×30`、点券 `1002 ×200`；状态变为已领取 |
| `001873/001874` | 本次实际投喂 | 消耗元气糕 `1028 ×700`，获得幸运星 `1029 ×100`；成长值 `0 → 700`，当日投喂 `0 → 1`，解锁第一则手记 |
| `001881/001882` | 手记展示标记 | 第一则手记标为已播放 |
| `001925/001926` | 用户在小程序领取第一则手记 | 泡泡棉花糖种子 `29004 ×1`、狗尾草种子 `20516 ×8`、有机化肥（12小时）`80014 ×1`；手记变为已领取 |
| `003215/003216`、`003261/003262` | 用户查看好友活动 | 自身活动 `data` 已显示成年、永久宠物已领取、寻宝 1 次、护送中 1 份；好友 `info.treasures` 为空，双方数据分别解码 |
| `003505/003506` | 护送日志 | 真实日志为空；响应同时确认宝藏价值 350、保底 50、博弈上限 500、被挑战 0/3、开始与结束相差 4 小时；当前锦囊 104「胜利加成」 |
| `000468/000469`（新会话） | 实际付费刷新锦囊 | 命令 41，消耗点券 `1002 ×30`，免费次数已用 1、付费次数已用 1；新候选 101/105，原锦囊 102 继续生效，等待选择 |
| `000473/000474`（新会话） | 用户保留当前锦囊 | 命令 42 传入当前锦囊 102（不在新候选中），响应仍装备 102，`charm_pick_used` 变为 true，无额外扣费 |

本次实际投喂前后，小程序显示元气糕 `1409 → 709`、幸运星 `450 → 550`、成长值 `0 → 700`，与响应字段一致。此次验证没有花费钻石。手记礼包由用户在小程序中点击照片后领取；官方客户端关闭照片时会自动发出领取请求。

新增会话为 `session-1789021262201`。本次仅实际执行一次 30 点券刷新；刷新后下一次按钮仍为 30 点券，与官方固定费用配置一致。刷新和保留原锦囊的四条消息体及官方 decoder 预期字段保存在 `core/tests/fixtures/pet-charm-refresh-capture.json`。

商城真实响应确认：商品 `1044` 使用点券 `1002 ×25`，商品 `1050` 使用金豆豆 `1005 ×150`；商品 `1045` 使用钻石 `1004 ×25`。活动适配不会自动购买这些商品。

## 宝藏护送

- 比熊之家新增护送入口和护送中数量；独立弹窗提供当前护送、宝藏详情、日志、固定锦囊总览、玩法说明及待领奖励。官方名称为“宝藏护送”。
- 当前护送展示服务器宝藏价值、被挑战次数和倒计时。宝藏详情区分待护送、护送中、待领取和已领取，提供初始价值、价值变动、保底资金、当前博弈资金、博弈上限、获得/开始时间及关联锦囊。挑战上限 0 按官方客户端显示“不限”。
- 多份已完成奖励按道具合计。只有状态 3 或状态 2 且有效结束时间已到时才可领取；服务端操作前再次核对。等待中的宝藏、结束时间 0 和重复领取均不会发送领奖请求。
- 日志使用真实命令 44，保留时间、挑战方、挑战书、等级、对应宝藏、双方锦囊、胜负、假宝藏标记及增加/减少的道具。互动记录和被挑战记录分开保存，避免切换页面时互相覆盖。
- 锦囊总览固定展示官方目录 5 种锦囊，当前生效项置顶，显示完整说明和生效次数限制；每日免费刷新 1 次，之后每次固定 30 点券、每天最多付费刷新 3 次，剩余次数来自服务端。刷新后展示两个新锦囊及当前锦囊，允许替换或保留原效果；待选时重新打开不会重复扣费。玩法说明直接读取活动响应的 `desc.tips2`，不另行编造完成条件。
- 弹窗改为自绘样式：标题栏、功能入口、锦囊卡片、宝藏详情与日志统一使用纸感底色和蜂蜜棕强调色。护送场景由内联 SVG 组件 `PetEscortLandscape.vue` 绘制，比熊复用 `PetMascot`，宝箱、爪印和光点都是 SVG 图元；护送中比熊沿地面往返、宝箱轻微起伏，关闭弹窗或 `prefers-reduced-motion: reduce` 时全部静止。不再引用官方标题、背景、宝箱、小车、车轮和步行帧贴图。
- 护送区域在 1280 / 768 / 390 / 320px 实测无横向溢出：功能入口按钮在 320px 下自动换行，宝藏价值栏可换行，弹窗正文可滚动到底部，底部文字操作高度 32px 便于点按。
- `pet-treasure-capture.json` 保存上述真实响应，并与独立官方 decoder 逐字段比较。非空日志、排队宝藏、完成后的领奖和并发重复领取使用明确标记的合成状态验证；没有将这些回放操作当成真实游戏操作。

## 操作一致性与资源限制

- 一键务农确认文案简化为“确定要一键务农吗？”，移除随活动变化而过时的固定清理项目列表。
- 活动操作共用串行写入队列，执行前重新查询活动状态和库存，检查每日次数、领取状态、限购和成本；成本按同币种合并后验证。
- 活动兑换拒绝钻石成本和钻石替代成本。锦囊付费刷新在串行操作内重新核对次数和点券库存，余额未知、点券不足、达到上限、尚未完成选择时均不发送付费请求。面板提交明确的免费/点券意图及预期付费次数，旧免费点击不会升级为收费，旧付费次数不能重复使用；拒绝钻石覆盖参数。官方命令 41 的请求体仍为空，面板前置校验参数不会发给游戏服务端；协议不提供币种锁，无法阻止其他客户端并发消费导致的服务端币种变化。
- 操作成功但随后刷新失败时，明确返回成功与刷新失败信息，前端暂停继续消耗，避免误重试。账号切换会清空详情，并丢弃旧账号尚未返回的结果。
- 萌宠查询与操作的 worker 超时已按串行 RPC 调整，避免被原先 10 秒 IPC 超时提前截断。
- 比熊成年后的永久宠物目录已加入 `90031`、官方头像、活动获得方式和技能“比熊润田”。服务端尚未返回该宠物时，宠物页仍显示“未获得”；拥有、上场状态仅由真实宠物响应决定，活动成年不会自行伪造已拥有状态。

## 分支范围

本活动 PR 以当前上游 `master` 为基线，保留其调度、网络发送、worker 身份初始化与断线诊断实现。Worker 文件只增加四个活动 API 的分发和对应调用超时；通用通信修复、个人页背包/任务加载修复及 main 独有队列不在 PR 最终差异中，继续保留在自己的 main。

## 验证范围

- 本轮浏览器回放验证免费/付费入口、两张新锦囊、保留当前、替换、重新打开不扣费、余额不足/未知/次数耗尽禁用和成功提示自动清理。改版后界面不再加载任何位图动图，动效全部由内联 SVG / CSS 实现。

- 本分支后端完整测试：142 项通过，0 失败。包含官方抓包解码、真实投喂/手记领奖回放、付费锦囊刷新和保留、护送状态、库存/次数限制、重复操作拦截及变异配置；上游原有测试继续保留。
- 前端活动状态回归测试：2 项通过，覆盖成功提示计时、账号切换清理和错误提示保留。
- 后端与客户端 TypeScript 编译、前端类型检查、活动实现及测试文件 ESLint、Vite 生产构建通过。上游 `worker.ts:404` 原有 `no-useless-return` 检查提示保持不变，已用同一配置比对上游原文件与本分支，结果一致。
- 改版后用本地 Playwright 在 1280px / 768px / 390px / 320px 复核四个栏目与护送弹窗：文档无横向滚动，页内无横向溢出元素，底部栏目四按钮等宽等高（52px），照片框保持 1:1，小铺商品卡等高，比熊之家 / 手记 / 小铺 / 节令 / 护送弹窗均无文字纵向截断，全部图片正常加载。抓包回放/合成账号与真实账号严格区分，本地回放不连接游戏服务器。
- 宠物页分别核对比熊“未获得”与“上场中”：未获得时操作禁用，已拥有并上场时显示“收回”；官方头像、天工品质、忠心护主及比熊润田均正常。未改共享 `PetPanel` 布局。
- 护送回放覆盖待护送 1 / 护送中 1 / 待领取 1 的混合队列，领取 275 幸运星仅产生一次本地请求，完成后按钮禁用；日志详情正确显示中级挑战书、75 幸运星损失及双方锦囊。Esc 关闭弹窗后恢复入口焦点。返回活动列表后，外层恢复 `overflow-y: auto` 和原来的手机内边距。
- 真实抓包保存在 `core/tests/fixtures/pet-diary-capture.json`、`pet-charm-refresh-capture.json` 等 fixture 文件；寻宝、夺宝等未覆盖的后期消息用官方独立 encoder 生成的合成向量补充验证，位于 `pet-diary-official-vectors.json`。这些合成向量不表示真实账号已完成对应操作。
- 已取得成年和护送中状态的真实响应，并核对实际小程序护送界面、规则与锦囊总览。尚未用真实账号跑完夺宝、护送结束后的领奖完整成功链路。好友排行榜依赖小程序开放数据域，面板未复制排行榜或开场/回顾动画。

本地测试日志和截图保存在 `output/`，不属于应用运行所需资源。

## 上游活动分支与发布版本

- `feat/pet-diary-upstream` 基于 `liyangpengs/qq-farm-bot` 的 `master`（`707a47c`）加入本次活动。保留上游活动服务结构和已有道具、作物、变异配置。
- 项目根目录、后端和前端版本统一为 `20260911`，默认游戏协议版本为 `1.14.0.3_20260909`；活动 ID 保持官方的 `2026090101`。
