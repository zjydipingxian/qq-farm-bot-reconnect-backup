# TSDK Node.js 运行约定

## 版本与来源

项目当前使用 `core/src/utils/tsdk.wasm`，来源为 2026-09-24 解包的官方 QQ 小游戏 `1.14.2.11_20260922` 中的 TSDK `v3.9.0.1790160550`，文件大小为 161,081 bytes，SHA-256 为：

```text
2c9e377ecc9a4fd9019f12191b589d543a60d6654580eb3e237b35f1fa5b1cb7
```

运行时固定参数：

- QQ 小程序 App ID：`1112386029`
- 微信小程序 App ID：`wx5306c5978fdb76e4`
- TSDK gameId：`3167`
- appKey：`0`

App ID 和宿主输入按账号平台选择。QQ 使用官方桌面端的设备文本 `windows;windows;windows 10.0;0;`、用户目录 `qqfile://usr/`，并在宿主不存在 `getAppBaseInfo` 时返回 debug 状态 `0`；微信账号继续使用项目设备配置、账号隔离目录和微信 App ID。QQ 虚拟用户目录收到的文件访问仍会安全映射到该账号自己的本地 TSDK 数据目录。

## 2026-09-24 升级验证

新版本的哈希、初始化和加解密往返测试通过；代理会话 `session-1790215551955` 中两个账号的官方一次性凭据与 Node.js 输出均逐字节一致（各 112/112 bytes）。测试向量已更新。此次未调整 ACE 或任务调度机制，新版本尚未完成长时间挂机验证。活动与商城验证范围见 [本次更新记录](autumn-20260924.md)。

## 2026-09-14 升级评估（历史）

2026-09-14 从 `v3.9.0.1788165223` 升级至 `v3.9.0.1789137379`，对应游戏包 `1.14.0.4_20260911`，完成了以下静态和离线核对：

- 22 个 `a.a` 至 `a.v` 导入与全部导出（`w`、`x`…`pa`、`decrypt_all_data` 及 mergewasm 段）与旧构建完全一致，ABI 没有变化；
- 沿用现有初始化常量（`MERGED_DATA_KEY=1871261153`、mergewasm 元数据 `67371008/404`、`RUNTIME_TABLE` 和 QQ 宿主输入）可直接初始化，`H()`、`N()`、`ba()`/`ca()` 往返与调度入口全部通过；
- 新构建仍只通过宿主文件接口访问 `enable.key` 和 `xugg_il`，没有额外依赖 `tsdk/acesdk.wasm.br`；
- 当前官方抓包（会话版本 `1.14.0.4_20260911`）的请求体由新构建解密后仍还原为正确 protobuf，网关加密链路没有变化；
- `AntiData` 上报负载与旧构建同构，并且已经和官方抓包逐字节对齐。51 字节最小样例的字段布局：`07 05` + 4 字节 `FNV-1a` 校验和（覆盖第 14 字节起的全部内容）+ `00 00 00` + 1 字节上报计数 + `00 00` + 2 字节内容长度（负载长度 - 14，大端）+ 7 字节常量 `e3a427b72edf94` + 4 字节时钟段 + `02` + 10 字节常量 `3879b22d5d393adecb4b` + `d3 2d 67` + 12 字节基线内容。`M()` 按调用当时的取整秒封装负载、`N()` 读取一次，两者都与平台、App ID、账号目录、open_id 和绑定时刻无关；2026-09-14 官方会话的 `ws_00151_SEND`（该会话第 5 次上报，计数 04）在相同秒下与本地输出逐字节一致。此前“有 2 个字节无法解释”的说法来自按字节对比时的错位，实际是校验和与时钟段；
- `H()` 一次性初始化凭据随构建变化，旧构建的固定向量不再适用；测试中的向量已更新为内置构建的冻结值，但尚未与同构建的官方 QQ 会话逐字节对照。

本次同时把 `Login` 和 `Heartbeat` 请求体收敛为可测的纯函数（`buildLoginBody`、`buildHeartbeatBody`），并用官方抓包向量锁定：

- `Login`：官方抓包（`ws_00001_SEND.bin`）解密后为 73 字节。此前的实现虽然取值全部正确，但 protobufjs 按「调用方是否赋值」决定字段存在性，因此少写了 `report_data` 的 6 个空字符串字段、多写了 `device_info.screen_width=0`、并漏掉了空的 `extra`，线上是 61 字节。现已与官方逐字节一致；
- `Heartbeat`：`{gid, client_version:1.14.0.4_20260911, field_3:0}` 共 27 字节，与官方抓包（`ws_00114_SEND.bin`）逐字节一致。

本次升级留下的缺口：

- 发布窗口内没有取到官方 QQ 包，`v3.9.0.1789137379` 的版本号来自微信包 `tsdk.js`；同一次发布下 QQ 与微信构建的时间戳通常相差几分钟（例如 2026-09-09 一档为 `1788164911` 与 `1788165223`）；
- 一次性初始化凭据的官方逐字节对齐结论只适用于 `v3.9.0.1788165223`，需要新一轮 QQ 官方会话才能恢复到当前构建。

## v3.9.0.1788165223 升级评估（历史）

从 `v3.9.0.1787640848` 升级至 `v3.9.0.1788165223` 后完成了以下静态和离线兼容性核对：

- 配套 QQ `tsdk.js` 明确返回 QQ App ID，并暴露了官方桌面端的用户目录、设备文本和 debug 状态；
- WASM 的 22 个 `a.a` 至 `a.v` 导入和关键 ABI 保持兼容；
- 初始/最大内存仍为 1,029 页，17 个 mergewasm 数据段的地址和长度保持不变；
- 运行时先解密 mergewasm 元数据段，再调用官方 `decrypt_all_data()` 完成其余数据段解密；
- QQ 宿主检测结果在标准 Node.js 中仅索引 `1` 与官方参考状态不同，运行时在校验其余 63 字节完全一致后才规范化该位；
- 文件校验哈希已同步更新，源码运行、TypeScript 编译和 `pkg` 打包仍使用同一资源路径。

本次升级没有改动网关 protobuf、Token 消费时序、ACE 调度或业务调用接口。修复部署后已连续观察约 23 小时未掉线，这一结果说明对齐线上真实行为可能提高会话稳定性，但仍不能据此断定掉线问题的唯一原因。

升级后的本地验证结果：

- 新 WASM 哈希校验通过；
- mergewasm 元数据解密、`decrypt_all_data()`、`x()` 和 `G(3167, "0")` 初始化通过；
- QQ/微信平台分别加载对应 App ID；
- QQ `H()` 的完整解码字节已加入固定测试向量，不仅检查字符串长度；
- `ba()`/`ca()` 加解密往返通过；
- TypeScript 构建和 TSDK 回归测试通过。

## Node.js 宿主映射

WASM 导入 `a.a` 至 `a.v`。`core/src/utils/tsdk-runtime.ts` 根据官方 `tsdk.js` 提供以下宿主能力：

- 断言、abort 和内存错误；
- 账号隔离的文件读写及 stat；
- JavaScript 调用栈；
- TSDK 版本、App ID、设备信息和运行时固定表；
- wall clock、monotonic clock 和服务端时间校准；
- TQOS HTTPS 上报。

Node.js 无法提供真实的小游戏触摸、陀螺仪、ACEVM 和函数完整性上下文。这些入口返回官方接口允许的空结果，并仅记录一次降级日志。QQ 一次性初始化凭据所依赖的宿主输入已与官方桌面端对齐；微信宿主仍需独立的官方会话继续核对。

## 关键 ABI

| 能力 | WASM 导出 |
| --- | --- |
| memory | `w` |
| create buffer | `A` |
| destroy buffer | `B` |
| init runtime | `G` |
| encrypted init info | `H` |
| heartbeat tick | `M` |
| data to server | `N` |
| data from server | `O` |
| process received data | `P` |
| encrypt in place | `ba` |
| decrypt in place | `ca` |
| speed detection | `fa` |

实例化后先使用 `__mergewasm_shared____wasm_decrypt_strings` 和固定密钥准备位于高内存的 mergewasm 元数据段，再调用 `decrypt_all_data()`、官方启动导出 `x()`，最后调用 `G(3167, appKeyPtr)`。不能在元数据尚未准备时直接调用 `decrypt_all_data()`，否则间接函数表尚未恢复，会触发 `table index is out of bounds`。

## 网关 Token

`gatepb.Message` 的 field 3 为 `token`。登录及普通业务请求使用随机 Token：生成 64～127 个字母数字字符并追加 `=`，总长度为 65～128。

官方客户端在初始化阶段还会产生一种不同格式的一次性 TSDK 凭据。QQ 抓包中的该凭据为 152 字符、Base64 解码后 112 字节，不适用普通 Token 的 65～128 字符及纯字母数字规则。账号绑定后读取一次 `H()`，并以原子方式优先用于下一条完成编码的请求，随后恢复随机 Token；三次连接中它分别落在初始化业务请求或重连 Login，证明不能按固定 RPC 名称注入。2026-09-03 使用三次官方会话及其各自对应的 TSDK 版本逐字节复核，修正后的 QQ Node.js 输出均为 112/112 字节完全一致；当前内置版本又直接通过了两次同版本官方会话的完整比较。

2026-09-14 复核确认该凭据的长度规则：Base64 解码后的字节数为 `80 + openId 字符数`。在固定宿主输入下扫描 openId 长度可得到 84/88/92/96/100/104/108/110/111/112/113/114/116 字节的连续结果，因此官方 152 字符/112 字节对应 32 字符 openId（QQ 标准 openId 长度），而回归测试里 136 字符/102 字节的冻结向量对应 22 字符测试 openId——两者一致，不是版本差异。凭据正文为高熵内容，不包含 openId 明文，逐字节对齐仍依赖同构建官方会话。

## ACE 生命周期

登录成功后启动 `core/src/services/ace.ts`：

- 每 5 秒读取 `N()`，首次读取发生在调度启动约 5 秒后；
- `N()` 非空时发送 `gamepb.acepb.AceService.AntiData`；
- 服务端 `AntiDataReply.result` 非空时传入 `O()`；
- 每 5 秒调用 `P()`；
- 每 25 秒调用 `M()`；
- 每 30 秒执行速度检测；
- 每 150 秒发送 TSDK 状态。

本次官方会话共 6 次非空 `AntiDataRequest.data`，分别为 278/186/85/207/51/176 字节，其中只有 `ws_00102` 的 `AntiDataReply.result` 非空（1081 字节）需要回灌。上报间隔完全由 `N()` 是否返回数据决定：负载只在 `M()` 时按当时的秒封装，读取一次后必须等下一次 `M()` 才会产生新负载，所以比 `M()` 更密的 poll 只会拿到空负载，既不能硬编码成 10 秒，也不能靠连续读取补发。只有首次成功回灌非空 result 后才记录“AntiData 链路正常”。

同一账号只允许一个 AntiData 请求在途。网络清理、重连和账号停止时会停止 ACE 调度并销毁当前 WASM 实例；下一次连接重新初始化。

## 多账号和资源路径

每个账号运行在独立 Worker 中，因此 CommonJS 模块、WASM 内存和调度器天然隔离。TSDK 可写数据保存到：

```text
core/data/tsdk/<accountId>/
```

WASM 通过 `getResourcePath('utils', 'tsdk.wasm')` 加载，兼容源码运行、TypeScript 编译目录和 `pkg` 资源打包。

## 已知限制与待补证项

- Node.js 无法与小游戏的传感器及 JavaScript 完整性环境完全等价。
- 运行时实现以反编译 `tsdk.js`、WASM ABI 和 WSS 抓包为依据。
- 一次性初始化凭据已经按“账号绑定后、下一条完成编码的请求消费一次”实现；并发初始化请求之间不依赖固定方法名。
- QQ 宿主输入和初始化凭据在 `v3.9.0.1788165223` 上已经逐字节对齐；微信目前只完成平台隔离和正确 App ID 选择，仍需微信官方会话做独立字节复核。
- 当前内置构建 `v3.9.0.1789137379` 的初始化凭据尚未与同构建的官方会话对照，测试里的冻结向量只能用于锁定 WASM 版本，不能当作官方对齐证据。
- `AntiData` 负载第 21～24 字节的时钟段只由取整秒的宿主时钟决定、本地可复现，但具体编码尚未解码；校验和、长度、计数和其余常量字段都已逐字节验证。
- `GateMessage.meta.server_seq` 在真实客户端中有时取已知最大值、有时取 0，目前没有足够证据恢复置零规则，代码保持现状。
- 2026-08-21 官方会话覆盖 3 次连接和 12 条业务 Heartbeat；请求间隔稳定在约 25 秒，线上的 protobuf 字段为 `gid`、完整 `client_version` 和显式 `field_3=0`。

## 2026-09-03 版本生产验证历史

2026-09-14 使用一次性 Code 对 TSDK `v3.9.0.1789137379` 跑了约 23 分钟的生产会话冒烟，登录后启动了农场与好友自动化：

- WSS 登录成功，`是我哟i`（Lv95）正常进入，服务端接受新构建加密的 Login；
- 一次性初始化凭据为 152 字符，与官方 QQ 会话的长度口径一致（`80 + openId 长度`，即 32 字符 openId）；
- 25 秒 Heartbeat 全程正常：`heartbeat_tick` 55 次、`speed_check` 46 次、`status_report` 9 次，均无失败；
- `anti_data` 调度 275 次，全程没有上报失败、协议错误、ACE 错误、踢下线或异常断连；
- 该窗口内服务端没有下发非空 `result`，回灌路径未被触发，仍未达到“AntiData 链路正常”的完整判定条件；`anti_data` 只统计调度次数，实际发出次数取决于 `M()`/`N()` 的产出节奏，目前没有单独埋点；
- 测试账号、TSDK 数据目录和统计文件在结束后均已删除。

2026-09-03 对 QQ 小游戏 `1.13.3.16_20260826`、TSDK `v3.9.0.1788165223` 完成官方会话审计：

- 三次会话的一次性初始化凭据均为 152 字符、解码后 112 字节，其中一次使用稍早的 `v3.9.0` 构建、两次使用当前构建；
- 使用每次会话对应的 TSDK、QQ App ID 和官方宿主输入后，三次 Node.js 结果均逐字节完全一致；当前内置版本也已与两次同版本官方会话直接比较通过；
- 审计时两条 WebSocket 在观察窗口内均持续收发，第二条由退出后重新登录产生，不属于异常自动重连；
- 修复后的 Node.js 会话已连续观察约 23 小时未掉线；该结果只支持“对齐线上真实行为可能提高稳定性”，不作为唯一因果或永久稳定的结论。

抓包中的完整 URL、Code、Token、OpenID 和账号标识不写入本文档，也不上传。

2026-08-26 使用小游戏 `1.13.3.11_20260826` 和 TSDK `v3.9.0.1787640848` 完成持续协议监听与活动实机操作：

- Login、Heartbeat、背包、好友、活动、天气、道具使用、收获和研究操作均返回成功；
- 雷雨召唤瓶、7200 秒天气状态、闪电变异配置 `12`、任务奖励和研究节点写操作均由生产回包确认；
- 会话中持续产生 `AceService.AntiData` 请求与成功响应，没有出现协议错误、踢下线或异常断连；
- Helper 仅接管并记录 QQ 流量，未修改 QQ 代理设置，活动操作期间监听持续写入新帧。

抓包中的完整 URL、Code、Token 和账号标识不写入本文档，也不上传。

2026-08-21 使用 Helper v0.2.2 保存完整握手 URL并连续观察官方登录、种地、重新登录、等待和退出：

- 三次握手、Login 和 Heartbeat 的版本均为 `1.13.2.10_20260723`；
- 三次握手除 Code 外完全一致，三个 32 字符 Code 均不同，确认官方“重新登录”获取 fresh Code；
- 共保存 432 条双向消息，其中 12 条 Heartbeat 约每 25 秒发送一次，字段 1、2、3 均实际出现在 wire 中；
- 每条连接只消费一次同一 TSDK 初始化凭据，普通请求继续使用 65～128 字符随机 Token。

抓包中的完整 URL、Code、Token 和账号标识不写入本文档，也不上传。

2026-08-20 使用全新的一次性 Code 对 TSDK `v3.9.0.1787056896` 做了约 38.5 秒的最小化生产冒烟。测试入口没有保存账号，也没有启动农场、好友、出售等自动化，只覆盖登录和安全链路：

- WSS Code 登录成功，服务端接受新版 TSDK 加密的 Login；
- 登录后的背包和用户设置请求成功；
- 25 秒业务 Heartbeat 请求及响应成功；
- TSDK 产生一次 51 字节非空 `AntiDataRequest.data`，服务端正常接受且返回成功响应；
- 该次 `AntiDataReply.result` 为空，因此没有触发回灌，也未达到“AntiData 链路正常”的完整判定条件；
- 测试期间没有协议错误、ACE 错误、踢下线或异常断连，结束时由测试入口主动关闭连接。

这次结果确认新版 TSDK 的生产 Login、普通加密请求、首次 AntiData 上报和 Heartbeat 基础链路可用。由于观察窗口内没有产生第二个非空 AntiData，也没有收到非空 result，仍需后续较长会话继续覆盖非空回灌和回灌后的稳定性。

## 历史生产链路验证

2026-07-28 使用短效登录 Code 对生产网关进行了约 35 秒的实际验证。该记录只适用于旧 TSDK `v3.8.6.1784551013`，不能视为后续 `v3.9.0` 构建的生产验证。历史结果如下：

- 旧 WASM 完成数据段解密和初始化；
- WSS 登录成功，服务端接受 TSDK 加密请求和网关 Token；
- 登录后背包和用户设置请求成功；
- 运行期间未出现 WSS 协议错误、ACE 错误或踢下线；
- 验证专用账号目录在结束后已删除。

历史 `v3.9.0.1787640848` 已覆盖 Login、普通请求、天气活动完整写链路、持续 AntiData 和 25 秒 Heartbeat；`v3.9.0.1788165223` 已完成官方凭据逐字节对齐、初始化和加解密离线验证，并连续运行约 23 小时未掉线。历史 `v3.9.0.1789137379` 已完成静态 ABI 核对、初始化和加解密离线验证，并补了一轮约 23 分钟的生产会话冒烟（见上），但还没有小时级的长时间观察，也缺少同构建的官方凭据逐字节对照。后续仍需跨更多会话持续观察非空回灌稳定性和服务端主动断线行为。

当前 `v3.9.0.1790160550` 已完成两账号官方初始化凭据逐字节对齐与离线加解密验证，尚未完成长时间挂机验证。
